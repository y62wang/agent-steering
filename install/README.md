# Install

Installation automation is intentionally deferred.

This directory will contain scripts that:

- select a work or personal profile
- install or link adapter files into agent-native locations
- preserve agent-specific features without duplicating shared content
